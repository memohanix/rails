<h1>Add new Url</h1>
<%= form_for(@url) do |f| %>
<ul>
	<% @url.errors.full_messages.each do |msg| %>
	<li>
		<%= msg %>
	</li>
	<% end %>
</ul>
<p>
	<label for="url">Title</label>:
	<%= f.text_field :url %>
</p>
<p>
	<%= submit_tag "Create" %>
	<% end %>
</p>S